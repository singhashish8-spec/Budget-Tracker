# Copy this file to .env.local (gitignored) and fill in your key, then rebuild.
#
# Gemini API key for receipt/statement scanning. Get a free one at:
#   https://aistudio.google.com/app/apikey
# NOTE: this key is baked into the APK at build time and is extractable from
# the installed app — use a rate-limited key you can rotate, and don't publish
# the APK. See README "AI scanning".
VITE_GEMINI_API_KEY=
