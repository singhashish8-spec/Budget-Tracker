# Copy to .env.local (gitignored) and fill in once you have a backend.
# See README.md "Configuring the placeholders" for why this is a backend
# endpoint rather than a client-side LLM API key.
VITE_AI_PARSE_ENDPOINT=
